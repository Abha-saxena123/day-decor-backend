'use strict';

/**
 * service-available-at router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::service-available-at.service-available-at');
