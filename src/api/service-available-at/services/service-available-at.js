'use strict';

/**
 * service-available-at service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::service-available-at.service-available-at');
