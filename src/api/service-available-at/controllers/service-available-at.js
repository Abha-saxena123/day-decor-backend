'use strict';

/**
 * service-available-at controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::service-available-at.service-available-at');
